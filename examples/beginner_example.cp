fn main() -> i32 {
    let x: i32 = 10;
    let y: i32 = 20;
    let sum: i32 = x + y;

    println("Hello from C-Prime!");
    println(sum);

    if sum > 20 {
        println("sum is greater than 20");
    } else {
        println("sum is not greater than 20");
    }

    return 0;
}
